Requirements
 -Node.js with NPM.
 -Internet Connection.

Steps to  run the whatwhowhere app:

1. Clone the folder of application from github in your system.
2. Open the nodejs command promt.
3. Go inside the folder with command “cd folderName”. 
4. Now install all the devDependecies in your system which are mentioned in package.json with the command “npm install”.
5. Run command 'node index.js' then app will be automatically loaded in browser.
